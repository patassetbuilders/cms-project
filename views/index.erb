<% if session[:message] %>
  <p><%= session.delete(:message) %></p>
<% end %>

<p>Files in the data folder</p>
<% @contents.each do |file_name| %>
  <a href="<%= file_name %>"><%= file_name %></a>
  <br />
<% end %>